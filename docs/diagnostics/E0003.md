# E0003 — MISSING SEPARATOR

Two word-tokens sit next to each other with no separator.

Identifiers, keywords, numbers, and strings must be separated by whitespace or a special symbol (§1.9). Insert a space or a newline. `endx` is an identifier, not `end` followed by `x`.
