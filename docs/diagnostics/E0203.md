# E0203 — WRONG ASSIGNMENT

`:-` was used on a value type. Use `:=` instead.

Reference assignment (`:-`) requires a `ref(Class)` or `text` on both sides. For `integer` / `real` / `boolean` / `character`, write `x := expr`.
