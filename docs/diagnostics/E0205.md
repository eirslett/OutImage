# E0205 — WRONG NUMBER OF ARGUMENTS

A procedure was called with the wrong number of actual parameters.

Count the formals on the procedure heading (or the Standard/BASICIO signature). Extra or missing arguments are illegal. `OutText` takes one text value; `OutInt` takes `(i, w)`.
