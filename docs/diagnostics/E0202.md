# E0202 — WRONG ASSIGNMENT

`:=` was used on an object reference. Use `:-` instead.

`ref(Class)` variables hold a pointer to an object, not a value. Reference assignment is `x :- expr` (§3.6). `:=` is only for value types (integer, real, boolean, character).
