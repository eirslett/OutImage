# E0408 — NOT A PREFIX

This class is not on the object's prefix chain.

`this Class`, `obj qua Class`, `obj is Class`, and `obj in Class` require `Class` to be the object's class or a prefix of it (§3.3).
