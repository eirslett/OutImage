# E0801 — LINKER FAILED

The host linker could not produce an executable.

Read the notes for the missing library, undefined symbol, or SDK path. On macOS install Xcode Command Line Tools; on Linux install `cc`; on Windows use an MSVC Developer Prompt. Override the driver with `SIM_LINKER`.
