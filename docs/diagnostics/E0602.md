# E0602 — EXTERNAL SPEC

A non-Simula external procedure has no `is procedure` specification.

Write `external C procedure Foo is procedure Foo; begin end;` so the compiler knows the Simula signature.
