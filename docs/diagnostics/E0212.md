# E0212 — CONSTANT

This constant initializer is not a legal constant expression.

A constant needs an initializer. The expression may only use literals and simple identifiers from outer scope — not variables from the same block head.
