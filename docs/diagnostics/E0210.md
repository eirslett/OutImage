# E0210 — NOT AN EXPRESSION

A statement procedure was used where a value is required.

`OutText`, `OutImage`, `fileWrite`, and similar BASICIO/filesystem procedures are statements. Call them as statements, or use a typed function procedure that returns a value.
