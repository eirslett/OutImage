# E0306 — DUPLICATE DECLARATION

The same name is declared twice in one block head.

A block may declare a name only once (§5). Rename one of the declarations, or move one into a nested block if you intend to shadow.
