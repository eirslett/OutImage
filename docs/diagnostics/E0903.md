# E0903 — UNDEFINED LABEL

A `goto` target was not found on the call stack.

A runtime `goto` can only land on a label that is still active in this procedure or an enclosing one. If analysis missed the label, this is also reported as E0304 at compile time.
