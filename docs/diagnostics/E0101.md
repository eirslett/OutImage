# E0101 — UNEXPECTED TOKEN

The parser was in the middle of a construct and hit something else.

Read the snippet: it shows what was found. The note lists a few tokens that would have been legal. Often a missing `;`, `end`, or `)` earlier is the real cause.
