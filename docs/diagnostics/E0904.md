# E0904 — ARRAY INDEX

A subscript is outside the array's declared bounds.

Each subscript must lie between the corresponding lower and upper bound (§9.3). Empty dimensions reject every access. Check the indices, or widen the declaration.
