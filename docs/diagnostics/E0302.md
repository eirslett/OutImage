# E0302 — UNKNOWN NAME

This procedure call does not name a known procedure.

Declare the procedure, prefix the block with a class that provides it (for example BASICIO for `OutText`), or import it with `external procedure`. Check spelling; Simula matching is case-insensitive.
