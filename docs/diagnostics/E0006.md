# E0006 — INVALID ISO CODE

An ISO character code in a string is out of range.

Inside a string, `!ddd!` inserts ISO character `ddd` with `0 ≤ ddd < 256`. Fix the digits or write the character another way.
