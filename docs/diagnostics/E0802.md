# E0802 — LINKER NOT FOUND

No host linker or C compiler was found on PATH.

Install a toolchain (`xcode-select --install`, `build-essential` / `clang`, or Visual Studio Build Tools) or set `SIM_LINKER` to `cc`, `clang`, `ld`, or `link.exe`.
