# E0901 — ARRAY TOO LARGE

The array's bounds overflow what the runtime can allocate.

Simula array extents are dense. If the product of the dimensions does not fit in a signed 32-bit count, allocation is rejected. Narrow the bounds, or split the array.
