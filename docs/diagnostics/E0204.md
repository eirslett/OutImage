# E0204 — TYPE MISMATCH

An expression is the wrong type for this position.

The report names the role (if-condition, operand, argument, …) and the type that was found. Convert the value, pick a different operator, or change a declaration so the types agree. A common beginner mistake is `"a" + "b"`: `+` adds numbers; text concatenation is `&`.
