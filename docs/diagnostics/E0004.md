# E0004 — DIRECTIVE PLACEMENT

`%` processor directives must start at the beginning of a line.

A `%` that is not the first character of the line is not a directive. Move it to column 0, or write a comment instead.
