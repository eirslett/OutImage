# W0001 — UNUSED

A local, parameter, or label is never referenced.

Remove the declaration, or use the name. This is a warning (`sim check` still succeeds). Disable with LSP `enableUnusedLints` / `--no-unused`.
