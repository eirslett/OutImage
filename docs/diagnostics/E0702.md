# E0702 — NOT LOWERED

This construct is not compiled for native or wasm yet.

Use `sim run` to interpret it. Simulation sequencing that is not yet lowered for AOT also needs a `Simulation` prefix. The note holds the internal detail of what was rejected.
