# E0701 — NO SIMULATION

`hold`, `activate`, `passivate`, `wait`, `cancel`, or `time` needs an active Simulation.

Prefix the program (or enclosing block) with `Simulation` so sequencing is in scope. These procedures are not available in a plain block.
