# E0409 — HIDDEN

`hidden` was specified for an attribute that is not protected.

Only a protected attribute may be specified hidden (§5.5.4). Write `protected hidden`, or protect it in a prefix class first.
