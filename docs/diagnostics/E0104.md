# E0104 — WRONG ASSIGNMENT

`:=` and `:-` are different operators and are not interchangeable in every production.

`:=` is value assignment. `:-` is reference assignment. The parser expected one and saw the other. After types are known, the compiler also checks that `:=` is used with value types and `:-` with `ref` / `text` (see E0202 / E0203).
