# E0303 — UNKNOWN ATTRIBUTE

This class has no visible attribute or procedure of that name.

Remote access `obj.attr` looks up `attr` in the object's class and prefixes, honouring `hidden` / `protected`. Check the class heading, the protection specification, and spelling.
