# E0601 — EXTERNAL KIND

This `external` kind is not C, JS, or Host.

sim recognises `external C procedure`, `external JS procedure`, and `external Host procedure`. Simula-to-Simula imports omit the kind.
