# E0103 — MISSING END

A `begin` was never closed with `end`.

Simula blocks, class bodies, and procedure bodies are `begin` … `end`. The report underlines the still-open `begin`. Add `end` (and a semicolon if this is a statement).
