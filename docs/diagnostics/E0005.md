# E0005 — INVALID NUMBER

This numeric literal does not match Simula §1.5.

Numbers are unsigned integers, optional fraction after `.`, and an optional `&` / `&&` exponent followed by digits. Check for a dangling `.`, empty exponent, or a letter in the digits.
