# E0803 — TOOLCHAIN

The host compiler, object writer, or SDK failed before linking.

The note names the missing tool, invalid target, or write error. Install Xcode CLT, `build-essential`/`clang`, or Visual Studio Build Tools. Check that the output path is writable.
