# E0402 — HIDDEN ATTRIBUTE

A `hidden` attribute is not visible in this class.

`hidden` disables further matching in subclasses of the hider (§5.5.3). Use a public attribute, or access the object at a prefix level that still sees the name.
