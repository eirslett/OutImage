# Assignment fixtures

Simula sources for §4.1 assignment statements.

Working fixtures are exercised by `tests/assignments.rs` for parse, semantic
analysis, and (where applicable) evaluation.

Deferred cases live in `tests/fixtures/pending/assignments/` and are tracked by
`tests/pending.rs`.
