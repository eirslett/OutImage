! §4.10.1: prefixed block with actual parameters on block-prefix.;
begin
    class Point(x, y); integer x, y;
    begin
    end;
    Point(1, 2) begin
        OutInt(x, 0);
        OutImage;
    end;
end
