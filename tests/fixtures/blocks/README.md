# Block fixtures

Simula sources for §4.10 blocks and subblocks.

Working block behavior (plain `begin`/`end`, nested blocks) is covered by
`tests/fixtures/begin_end.sim`, `tests/compile.rs`, and parser unit tests.

Deferred cases live in `tests/fixtures/pending/blocks/` and are tracked by
`tests/pending.rs`.
