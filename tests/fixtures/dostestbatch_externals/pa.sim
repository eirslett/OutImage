% External integer procedure pa for SIMULA TEST NO 40.
% Not shipped in the TestBatch tree; original PA source.
integer procedure pa(n);
  integer n;
  begin external integer procedure pb;
    if n<10 then pa:= n+pb(n+1);
  end;
